<!--pages/about/about.wxml -->
<!-- <view class="container">

  <view class="item">
    <view class="item-left">
      <image class="picture" src="{{item1[0]}}"></image>
    </view>

    <view class="item-middle">
      <view class="title">{{item1[1]}}</view>
      <view class="introduction">{{item1[2]}}</view>
      <view class="performer">{{item1[3]}}</view>
    </view>

    <view class="item-right">
      <view class="score">{{item1[4]}}</view>
      <button class="buy" type="primary" plain="true" id="buy1" bindtap="buyTicket">{{buyticketTitle}}</button>
    </view>
  </view>

  <view class="item">
    <view class="item-left">
      <image class="picture" src="{{item2[0]}}"></image>
    </view>

    <view class="item-middle">
      <view class="title">{{item2[1]}}</view>
      <view class="introduction">{{item2[2]}}</view>
      <view class="performer">{{item2[3]}}</view>
    </view>

    <view class="item-right">
      <view class="score">{{item2[4]}}</view>
      <button class="buy" type="primary" plain="true" id="buy2" bindtap="buyTicket">{{buyticketTitle}}</button>
    </view>
  </view>
</view> -->

<!-- 单独定义一个模版 -->
<template name="firstTemplate">
  <view class="item-whole">
    <view class="item-left">
      <image class="picture" src="{{url}}"></image>
    </view>

    <view class="item-middle">
      <view class="title">{{title}}</view>
      <view class="description">{{content}}</view>
      <view class="authors">{{autor}}</view>
    </view>

    <view class="item-right">
      <view class="score">{{score}}</view>
      <button class="buy-tickets" type="primary" plain="true" bindtap="buyTicket">购票</button>
    </view>
  </view>
</template>
<view wx:for="{{itemArr}}">
  <template is="firstTemplate" data="{{...itemArr[index]}}" />
</view>
