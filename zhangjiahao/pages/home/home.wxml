<!--pages/home/home.wxml-->
<!--pages/home/home.wxml-->

<!--pages/home/home.wxml-->
<button bindtap="setDisabled" type="primary" size="mini"  plain="{{plain}}"
      > add </button>
<button type="primary" size="mini"  plain="{{plain}}"
      > sub </button>
<block wx:for="{{array}}">
      <view>{{index+1}} '{{item.mes}}'</view>
</block>

