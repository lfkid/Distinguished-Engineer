<!--pages/home/home.wxml-->
<!--pages/home/home.wxml-->

<!--pages/home/home.wxml-->
<button bindtap="addArray" size="mini" }"
      > add </button>
<button type="subArray" size="mini" }"
      > sub </button>
<block wx:for="{{array}}">
      <view>{{index+1}} '{{item.mes}}'</view>
</block>

